services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: nanjinghexi-school:latest
    container_name: nanjinghexi-school
    ports:
      - "49000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
